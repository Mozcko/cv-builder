---
import PublicLayout from '../layouts/PublicLayout.astro';
import HeroActions from '../components/ui/HeroActions'; // <--- Importamos el nuevo componente
---

<PublicLayout title="CVStudio - Crea CVs Profesionales con IA">
    <main class="flex-1 flex flex-col items-center justify-center text-center px-4 py-20 bg-gradient-to-b from-app-bg to-slate-900">
        
        <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-blue-500/10 border border-blue-500/20 text-blue-400 text-xs font-bold mb-8 uppercase tracking-wider">
            ✨ Potenciado por DeepSeek AI
        </div>

        <h1 class="text-5xl md:text-7xl font-bold text-white mb-6 tracking-tight max-w-4xl">
            Tu currículum profesional, <br/>
            <span class="text-transparent bg-clip-text bg-gradient-to-r from-blue-400 to-purple-500">optimizado en segundos.</span>
        </h1>
        
        <p class="text-xl text-slate-400 mb-10 max-w-2xl mx-auto leading-relaxed">
            Deja que la IA corrija tu redacción, traduzca tu perfil y optimice tus habilidades para pasar los filtros ATS. Sin complicaciones.
        </p>

        {/* AQUÍ ESTÁ EL CAMBIO: Usamos el componente React con client:load */}
        <HeroActions client:load />

        {/* Preview Image */}
        <div class="mt-20 relative w-full max-w-5xl">
            <div class="absolute -inset-1 bg-gradient-to-r from-blue-500 to-purple-600 rounded-2xl blur opacity-20"></div>
            <div class="relative bg-slate-900 border border-slate-800 rounded-xl overflow-hidden shadow-2xl h-64 md:h-96 flex items-center justify-center">
                 <div class="text-slate-600 font-mono text-sm">
                    [Vista previa del Editor]
                 </div>
            </div>
        </div>

    </main>
</PublicLayout>